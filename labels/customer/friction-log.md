# Friction log: customer rendering

Where the rubric did not decide the case on its own. One line per occurrence,
written during the pass, applied to the rubric afterwards.

The verdicts themselves are in [`items.csv`](items.csv) and [`runs.csv`](runs.csv);
which version of each axis a column was read against is the `rubric_commit` of
its own rows there.

### Re-passes after a rubric change

What was re-read when an axis changed, and what moved. A check with no movement
is a result too: without it there is nothing to show that the column was read
against the text it is now compared with.

| axis | read against | what moved |
| --- | --- | --- |
| C2 | f34e11b | `s5-26` stays `fail`: "in some runs" names no condition at all, which the widened rule still fails and now says so in as many words. `o5-13` moves to `pass`: "re-running the same tag" is a condition, and it no longer has to carry a number. |
| B1 | ce900ac | All three runs move from `fail` to `pass`. The axis now asks whether an entry demands something the reader pays for ignoring, not whether any action slot is filled. v0.1.0 has no breaking change and the entries name only optional configuration, so no entry is marked and rule 3 applies: nothing to put in the wrong order. |
| B2 | ce900ac | All three stay `fail`, on a reason the axis did not have before. `sonnet-5-out` and `sonnet-5-no-thinking-out` carry no group headings at all; `opus-5-out` has `Features` and `Fixes`, which are not the defined set. The old note on `sonnet-5-out` pointed at the most-consequential rule, which no longer exists, and was rewritten. |
| C3 | c57ec89 | Two of the 21 fails move to `pass`: `o5-04` and `o5-14`, whose closings were read as error-handling rather than as an outcome. Six more reasons were rewritten without the verdict moving - they argued from where the clause sat ("closes on ..."), which the axis stopped asking about when it went to substance only. The column now stands at 19 fail and 34 pass. |
| A1 | 9d61351 | Half one changed in wording only - "exclude" became "the entry fails" - and none of the five fails moves. Half two was added by the same commit and is recorded per run in `missing.md`, now also as an A1 row per run in `runs.csv`: all three renderings leave out an observable change, which rule 7 fails. |
| C1 | 9d61351 | One of the 30 fails moves: `o5-15` opens on a property of the output rather than on what was built, which the axis now carries as a pass example in as many words. The other 29 open on "Added ...", "Reworked ..." or the mechanism and stand unchanged. The bold label made no difference: the reasons already quoted the text after the colon. The column stands at 29 fail and 24 pass, and `o5-15` becomes the eighth shippable entry. |

#### The nine axes rewritten - `57f2900`

`57f2900` rewrote every axis to `rubric/README.md`, so every column was read
again against it and all nine now carry that commit. Four columns moved, five
were checked and stood. Counts are against the tables as they were before the
pass.

| axis | read against | what moved |
| --- | --- | --- |
| C1 | 57f2900 | 21 of the 29 fails move to `pass`: `s5-22`, and 20 of the 25 entries of `opus-5-out`. The axis stopped reading the opening for what it names - a setting, a file or a command in the first clause no longer sinks it - and rule 5 took the grammatical subject out of the decision as well. What is left is the rewrite test, which "Choose how much source material feeds the fact base via `Chartula:FactBase:Depth`" survives. Five more reasons were rewritten without the verdict moving; they had argued from a name being present. The column stands at 45 pass and 8 fail, and the two fails left in `opus-5-out` open on the filtering logic (`o5-06`) and on the instruction given to the model (`o5-16`), neither of which the reader meets. |
| C4 | 57f2900 | `s5-06` and `s5-14` move from `fail` to `n/a`, which closes the two cells the judge had disagreed on. The move is not carried by the axis's own text: rule 1 asks whether the *change* requires anything, and a release-notes credential is a step without which the change does not reach the reader; the re-pass argued from the *entry*, which announces no requirement. The verdicts now say what the labels mean and the rule that would produce them is still unwritten - see the C4 row under *From judge runs*. The column stands at 30 n/a, 15 pass, 8 fail. |
| A1 | 57f2900 | No verdict moves, and all 48 passes gain a reason where they carried none. Half one now asks what a reader would have to do to notice the change, so a pass has an answer to give: `s5-01` "runs `preview` or `generate` and sees the metrics report printed at the end of the run". An empty note was defensible while the axis only said what to exclude; against the current rule it hides whether the question was asked at all. |
| C5 | 57f2900 | No verdict moves. Two reasons that were empty now quote what they turn on: `o5-03` on the raw keys `categories`, `order`, `names`, `breakingProminent`, and `o5-08` on `GITHUB_TOKEN`, which is the axis's own environment-variable pass example. Both were already the labels' position; the axis now states the test they were using. |
| B2 | 57f2900 | No verdict moves - all three runs still fail - and the three empty notes now carry the passage. `sonnet-5-out` (lines 285-312) and `sonnet-5-no-thinking-out` have no group headings at all and fail rule 4. `opus-5-out` is the one rendering that has them, `### Features` and `### Fixes`, and it fails twice over: rule 3, because the document opens directly on the first group heading and the opening the format requires is missing, and rule 2, because neither name is in the set the format defines. Two of the three notes were written onto the wrong runs during the pass and were swapped back afterwards; no verdict was affected, and the reading is the one the `ce900ac` row above already recorded. |
| B1, B3, C2, C3 | 57f2900 | Read again against the rewritten axes; no verdict moved and no reason changed. |

#### B1 and C4 pulled apart, and Units - `7213389`, `a5a1c68`

`7213389` removed the sentence in which B1 and C4 each claimed to use the
other's test, and rewrote C4 rule 1 to read the entry. Those two columns were
read again in full. The other seven carry `a5a1c68` for a different reason:
`Units` goes into every prompt beside the axis, it changed there, and nothing
had been measuring it until `88b8f83`.

| axis | read against | what moved |
| --- | --- | --- |
| B1 | 7213389 | `sonnet-5-out` and `opus-5-out` move from `pass` to `fail`. Both carry an entry saying separate files stop being written - `s5-05` at position 5 of 26, `o5-12` at 12 of 25 - and both sit below entries that ask nothing, which is the axis's own fail pattern. The column had no marked entry at all before this pass; it now has two, and with them its first fails. `sonnet-5-no-thinking-out` stays `pass`: two fixes with nothing to set. |
| C4 | 7213389 | Five verdicts move, none of them the two the pass was expected to touch. `s5-05` goes `pass` to `n/a` and not the other way: with rule 1 reading the entry, the entry offers the reader nothing - no step, no setting, no decision - so the axis never opens, and the place it names stops mattering. `o5-10` and `sn5-02` move the same way. `o5-08` and `sn5-01` move `n/a` to `pass`, on `GITHUB_TOKEN` and `llm.maxOutputTokens` being named. The eight fails are untouched. |
| A1, B2, B3, C1, C2, C3, C5 | a5a1c68 | Nothing moved, and nothing could. The only change to `Units` since these columns were last read is the collapsed-line paragraph, which stopped citing a B3 rule and now points at the format document. The claim it makes is the same, and `s5-26`, the one entry in the corpus that gathers two changes into one, is not a collapsed line anyway - it carries full slots, which is why C2, C3 and C4 fail it with reasons rather than answering `n/a`. |

The C4 pass turned up a test the axis does not state. Four of the five moves
turn on whether a named thing is a lever over Chartula's own behaviour or a
convention it merely reads: `GITHUB_TOKEN` and `llm.maxOutputTokens` are levers
and pass, while closing keywords (`o5-10`) and the breaking-change footer
(`sn5-02`) are Conventional Commits and answer `n/a`. That distinction decides
four cells and appears nowhere in rule 1. It is the same species of finding as
the credential case: recorded, not decided.

### `sonnet-5-out`

| item                                | axis   | what was unclear                                                                                                                                                                                                                                                                                                                                                                                 | how it was decided                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
|-------------------------------------|--------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| s5-01                               | C4     | For an always-on feature, "where to find it" collapses into slot 1. Is that a pass, or does C4 not apply to features that need no action?                                                                                                                                                                                                                                                        | C4 is `n/a` where there is no action. Applied to the rubric.                                                                                                                                                                                                                                                                                                                                                                                                                      |
| s5-02 to s5-26                      | C4     | Same call repeated 23 of 26 times: the item names no location, no setting and no default status, so C4 fails. Read that way the axis fails almost everything and separates nothing. Does C4 need an action only where the user has one to take (opt-in, setting, command), with an explicit "works without setup" counting as satisfying it?                                                     | Same decision: the axis judges a withheld action, not a missing sentence. Fails remain where something can be set and the item does not say where. Applied to the rubric.                                                                                                                                                                                                                                                                                                         |
| s5-04, s5-13, s5-15, s5-18, s5-21   | C3     | Two different defects fire the same axis. Some of the 20 C3 fails state a real outcome and then trail past it into an edge case, a caveat or a footnote, so they fail on position, not on substance. The decision procedure only covers the other shape (closing restates slot 1 or the mechanism). Is "the item ends on the outcome" part of C3, or is a trailing caveat a B3 length finding?   | C3 measures substance only, or no minimal pair can be built for it. The six items pass C3; the trailing sentences are B3 rule 1 and are named in the run-table note. No new axis: one run is too little to justify one. Applied to the rubric. The re-pass then moved five items, not the six first suspected: s5-19, s5-20, s5-24 and s5-25 state no outcome at all and stay `fail`, while s5-04, s5-13 and s5-18 turned out to have the same defect and were not on the list.   |
| s5-01                               | C5     | `"the deeper (thorough) check"`: `deeper` is explanatory prose, `thorough` is the mode value. The rubric allows product surface a user encounters and forbids internal stage names and configuration values, and this expression is both at once.                                                                                                                                                | `fail`. It survives the paste test in rule 2 - `thorough` could be written into a configuration file as it stands - and `deeper` in front of it does not make the word mean anything to someone who has only used the product. Recorded because the opposite reading is defensible: the item does explain the stage in prose rather than only naming it.                                                                                                                          |
| s5-02, s5-10, s5-13, s5-22          | C3     | Not independent of C1. Where the item opens on the mechanism, the closing tends to restate the mechanism, and the same defect is counted twice, on C1 and again on C3. The minimal pairs are supposed to keep the axes separable; these items separate nothing. Does C3 judge the closing on its own terms once C1 has already failed?                                                           | No rubric change. The claim is that each axis has an item failing it alone, not that no item may fail two: s5-09 fails C1 with C3 passing, s5-03 fails C3 alone. Separability holds on observed items. Double counting changes nothing under the aggregation rule.                                                                                                                                                                                                                |
| s5-05                               | B1     | Slot 1 contrasts with an earlier location: the audience texts live in `changelog.json` "instead of being written as separate files in your repository". A reader whose process picks those files up pays for ignoring the entry, which is what rule 2 marks - and the entry sits fifth, so marking it would fail the run. | Not marked. `v0.1.0` is the first release, so no reader can have depended on the earlier location; the contrast is with an intermediate state of this release's own development, not with something that shipped. Recorded because the opposite reading is defensible and decides the whole run. |

C2 is read as f34e11b; s5-26 remains a failure.
B1 is read as ce900ac; the run moves from `fail` to `pass`.

### `opus-5-out`

| item                                | axis    | what was unclear                                                                                                                                                                                                                                                                                  | how it was decided                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
|-------------------------------------|---------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| o5-01                               | C1      | Opens with the command to type: `"Run chartula preview or chartula generate with --tag and --repo"`. That is neither a change, a setting nor a mechanism - the three things the decision procedure names - it is slot 4 pulled to the front. Does an instruction fail C1?                         | open                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| o5-01, and the C3 column as a whole | C3      | Judged with "closes on X, not an outcome" - the position reading that was dropped when the sonnet friction was applied. o5-01 says `"so you see the real result"` mid-item, the same shape `s5-04` was passed on. All 25 items are `fail`, so the column cannot separate anything as it stands.   | Re-read on substance: 25 fails became 6 (o5-04, o5-14, o5-16, o5-21, o5-23, o5-24), and every reason was rewritten to name the outcome the item does state. Nothing became shippable: C1 fails 23 of 25, and the two items that pass C1 fail elsewhere.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| o5-14, o5-16                        | C3      | Two fails whose neighbours pass on what looks like the same statement: o5-13 also states an idempotency guarantee, `s5-13` also states that the model invents nothing.                                                                                                                            | Fails stand, and the pairs separate on more than wording. o5-14 `"updates that one release rather than creating a duplicate"` restates the mechanism from the sentence before it (`"updates it in place if it exists, or creates it otherwise"`), and `"API and network failures surface as clear errors"` is the rubric's own fail example; o5-13 instead names a property the reader can check afterwards - a byte-identical file, an older release never moving above a newer one. o5-16 never leaves the instruction frame: `"the model is instructed to..."`, `"rather than something to change or infer"`, `"not decided by it"` all repeat its first sentence, while `s5-13` states the resulting property (`"only rephrases the given facts and never invents details"`). The test that separates them: an instruction given to the model is mechanism, the property the output then has is the outcome. |
| o5-04, o5-05, o5-06, o5-20, o5-21   | C4 / C5 | The same words decide both axes in opposite directions: naming `Chartula:FactBase:Depth` is what makes C4 pass, because the reader now knows where the setting lives, and what makes C5 fail, because it is a raw key. C5 asks for the setting in prose, which then leaves C4 without a location. | Decided, no label change. C4 asks for the plain name, not the key, so an item that names the setting in prose passes both axes. Where only the key is given, C4 passes and C5 fails, and the finding belongs to C5. Written into C4 as step 4.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| o5-02 to o5-25                      | C1      | 23 of 25 items fail, which is the volume that made C4 suspect on the sonnet run.                                                                                                                                                                                                                  | Checked, no rubric change. The reasons differ item by item and each names a real mechanism-first opening; the axis is separating, the run is simply that uniform. o5-23 and o5-24 pass, so it is not vacuous.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| o5-10, o5-17, o5-25                 | A1 / C5 | All three fail A1 for describing internal work, and all three pass C5 while doing it. If the change itself is internal, its wording is internal too - so why does C5 not fail with it?                                                                                                            | Decided, no label change. A1 judges the subject of an entry, C5 its language, and the two are independent: an internal change can be described in faultless customer language. Written into the Levels section, which governs A1 and C5 alike.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| o5-01, o5-12                        | B1      | Two candidates for a marked entry. o5-01 says `--repo` "must be given explicitly for now", and o5-12 carries the same location contrast as `s5-05` - the audience texts now live inside one file, "so no separate marketing files are written into your repository". | Neither is marked. A required flag is how the command is reached, which the axis excludes by name; the location contrast is decided as it was on `s5-05`. With nothing marked, rule 3 passes the run - the group order stays B2's finding. |

C2 is read as f34e11b; o5-13 moves.
B1 is read as ce900ac; the run moves from `fail` to `pass`.

### `sonnet-5-no-thinking-out`

| item                | axis                    | what was unclear                                                                                                                                                                                                                                                                                                             | how it was decided                                                                                                                                                                                                                                                                                                                       |
|---------------------|-------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| lines 475, 480, 482 | scope of the item table | The run carries three pieces of text that are neither bullet entries nor group headings: a heading line and two closing paragraphs. The rubric says every item is built from four slots but never says what counts as an item, so whether these get rows of their own or fall to level B is not settled by anything written. | Checked, no rubric change. The line content was linked to the items. Since this makes the item's affiliation less clear and leads to confusion when there are significantly more items, it has been decided that the scope does not belong in the list, but rather that the affiliation should be specified within the respective items. |

B1 is read as ce900ac; the run moves from `fail` to `pass`. Neither fix asks
anything of the reader, so rule 3 decides it; the omitted features are A1.

### From judge runs

Disagreements between a judge run and the labels, and what they showed about
the axis. The run is named so the verdicts can be looked up.

| axis | entries                                                 | what was unclear                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | how it was decided                                                                                                                                                                                                                                                                                                                                                                                                                         |
|------|---------------------------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| B1   | s5-05 (`sonnet-5-out`)                                  | The judge failed the run where the labels pass it. It marks `s5-05` under rule 2 - "instead of being written as separate files in your repository" is output changing under the reader - and then fails rule 4 on its position below four unmarked entries. Run `labelled-B1-claude-sonnet-5-2026-09-02T120659.json`. The two sides read different things: the labels read the release, where v0.1.0 is the first tag and nothing can break; the judge reads the document, where "instead of" states a prior arrangement that never existed. B1 is sent the document alone - no fact base - so the release is not reachable from the call. The labels had already written this reading down as the one that might come: the `s5-05` row under `sonnet-5-out` above closes with "Recorded because the opposite reading is defensible and decides the whole run". It came, from the judge, on the first run of the axis. | Decided: B1 reads the entry, not the release. An entry that contrasts with an earlier arrangement states that the arrangement existed, and whether it really did is the faithfulness check's question, which the rubric excludes in its opening - and the release is not in the call either way. `s5-05` is marked, and so is `o5-12`, which carries the same contrast; both sit below unmarked entries, so `sonnet-5-out` and `opus-5-out` fail B1. The column carried no marks at all before this, which is why its 2 of 3 measured nothing. |
| B1   | the column                                              | No entry of any run is marked, so the human column is three passes and the run reports `human_fail 0, caught 0`. Agreement of 2 of 3 says the judge was once stricter, not that it finds a B1 violation - the majority-class warning `run_labelled.py` prints applies in full. That it can find one is shown by the separation run, where a constructed B1 break was caught, not by this figure. | Nothing to decide - a limit of the corpus, to be stated beside the B1 figure wherever it is quoted. |
| C4   | s5-06, s5-14                                            | The judge answered `n/a` where the labels say `fail`, on the two entries where the credential is set up once and not per release. Run `labelled-claude-sonnet-5-2026-08-31T1552.json`.                                                                                                                                                                                                                                                                                                                      | the labels moved to `n/a` in the `57f2900` re-pass, so the disagreement is gone - but on a reading of the entry, not of rule 1, which asked about the change and would have called a credential a step. Closed by the rule 1 rewrite: the rule reads the entry now, and what the change carries unstated moved to the Open list. See the second `s5-06, s5-14` row below.                                                                                                                                                                                                                                                                       |
| C4   | o5-17                                                   | The judge passed an entry that places a setting "via configuration" and in "an environment variable"; the labels fail it, because rule 3 asks for the setting by its plain name, the command or the place, and a kind of place is not a place. Same run.                                                                                                                                                                                                                                                    | Open, and untouched by the rule 1 rewrite: the entry offers a setting under either reading, so rule 1 opens C4 on it either way. What is disputed is rule 3 - whether a kind of place is a place - and that sentence did not change. |
| C4   | s5-06, s5-14                                            | Judged again against `57f2900`, run `labelled-C4-claude-sonnet-5-2026-09-02T121958.json`: 51 of 53 agree, 7 of the 8 human fails caught, one false fail. The two cells now agree, and the reasoning is the one the re-pass used rather than the one rule 1 states - "no action, step, or setting required of the reader" on `s5-06`, "no config/step needed to receive this change" on `s5-14`. Both argue from the entry. | Decided, and not by widening C4: rule 1 now reads the entry, both entries state no requirement, and both stay n/a. What their change does carry - a credential without which it reaches nobody - is reachable from no axis that reads the entry, and covering it would ask the judge about something the prompt does not contain. It moves to the Open list, where it waits on the fact base A1's second half needs anyway. |
| C4   | s5-05 (`sonnet-5-out`)                                  | The judge fails what the labels pass, on the same entry and the same words as on B1: "alters where output files live, which readers relying on separate files must adapt to, but the entry never states what the reader should do about it", rule 3. Same run. It does not contradict the two n/a cells above: the replacement is stated in this entry and in neither of those, so the judge reads the entry throughout and is consistent across both axes. | Decided, and the two axes come apart rather than agreeing: B1 asks where a marked entry stands, C4 whether what the entry offers is reachably named. `s5-05` answers `n/a` on C4: with rule 1 reading the entry, the entry offers the reader nothing at all, so the axis never opens and the place it names never comes up. This row said `pass` when it was written, assuming rule 1 opens and rule 3 decides; the re-pass against `7213389` corrected that. B1 marks it either way. The judge's C4 fail is a false fail against the axis as written, not the symptom of an underdetermined axis - the two axes were never asking one question, they only said they were. |
| C4   | s5-26                                                   | The one human fail the judge missed. The labels fail it on "a configurable ceiling" named without a place, which is the axis's own fail example; the judge answers n/a - "a bug-fix entry with no step required of the reader" - stopping at rule 2 without reaching rule 3. The entry is the rubric's own worked example for the fix section. | Open, and not an unclear axis: rules 2 and 3 both apply and only the first was applied. Worth re-reading after any change to rule 1, since that is where it stopped. |
| C5   | s5-05, s5-17, o5-07, o5-08, o5-10, o5-16, o5-17, sn5-01 | Eight entries the labels pass and the judge fails, on `changelog.json`, `GITHUB_TOKEN`, the `(breaking)` marker and descriptions of pipeline stages. Run `labelled-claude-sonnet-5-2026-08-31T1612.json`.                                                                                                                                                                                                                                                                                                   | open                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| C5   | s5-01, o5-21, sn5-02                                    | Three the other way: the judge passed `(thorough)` and `Chartula:Review`, reading a configuration key as "a command the user actually types" - which is in the allowed list and is a hole wide enough for any key. Same run.                                                                                                                                                                                                                                                                                | open                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| C5   | the axis as a whole                                     | Both directions come from the same place: the axis decides by an enumerated list of allowed and forbidden kinds of expression, and that list cannot be finished - a generated file, an environment variable the user sets, a marker in the fact base and a mode value all fall outside what it names. It is the same defect C3 rule 2 had with its list of phrases.                                                                                                                                         | open. The candidate is a test rather than a list: would a reader have met this exact string by using the product - typed it, seen it in their repository, seen it on screen? Met only by reading the source or developer documentation, it fails. That would pass `changelog.json` and `GITHUB_TOKEN` and fail `(breaking)` and `Chartula:Review`, which is where the labels already stand. Written into the axis as rules 1-4 by `57f2900`; the re-pass moved no C5 verdict, so the labels the judge disagreed with are the ones the axis now produces. Closed here, open until a judge run confirms it. |
| C5   | the corpus                                              | Sharper than the list problem: every run describes Chartula's own release, so the product whose surface C5 judges is the tool whose internals the entries name. `chartula.yaml`, `GITHUB_TOKEN` and `Chartula:Review` are product surface to a Chartula user and source to whoever wrote them, at the same time. For a changelog about any other repository the two come apart and Chartula's own keys never appear. Part of the eight disagreements is therefore an artefact of the case, not of the axis. | open, and it points at a second thing the axis does not say: whose knowledge counts. The reader is a user of the product the changelog is about, never the person who built it. Stated that way, the entries the labels pass do pass. C5 stays judgeable here but is not cleanly measurable until a case from a repository other than this one exists. `57f2900` wrote both halves into the axis - whose knowledge counts, and the caveat about this corpus.                                                                                     |
